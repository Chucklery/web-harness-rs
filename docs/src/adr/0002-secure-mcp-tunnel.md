# ADR 0002: Use OpenAI Secure MCP Tunnel

Status: accepted.

The intended remote transport is OpenAI Secure MCP Tunnel rather than a custom reverse proxy or public local HTTP service.

Current tunnel setup details must be verified against OpenAI official documentation because product flags and UI can evolve.

