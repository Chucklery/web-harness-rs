# ADR 0003: Use Rust for the local host

Status: accepted.

Rust is used for predictable memory behavior, a small native deployment footprint, process and filesystem control, and alignment with the implementation language used by Codex.

The project does not require Node or Electron in the core runtime.

