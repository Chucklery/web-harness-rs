# ADR 0005: Do not depend on codex-core

Status: accepted.

The project may reuse small isolated algorithms or adapted code with attribution, but it does not depend on the complete codex-core runtime.

The goal is to reuse execution semantics without embedding another agent product stack.

